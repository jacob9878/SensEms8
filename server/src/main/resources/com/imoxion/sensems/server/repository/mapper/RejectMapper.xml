<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.imoxion.sensems.server.repository.mapper.RejectMapper">

    <!--<select id="getRejectList" resultType="com.imoxion.sensems.server.domain.ImbReject">
       select lower(email) as email, msgid, regdate from imb_reject
    </select>-->
    <select id="getRejectList" resultType="String">
        select lower(email) as email from imb_reject
    </select>

</mapper>